# Task 4.5 — Source-Frozen Shadow-Trial Protocol Hardening Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax for tracking. Each increment is committed separately after its own RED→GREEN cycle and focused verification.

**Goal:** Close five verified runtime-protocol gaps before the first real qualification day so that Task 5 becomes a precise, recoverable, source-frozen qualification protocol: terminal-report safety, crash-safe day closure, market-day (not calendar-day) leases, one immutable runtime identity across all six countable days, full paper/reconciliation cross-binding, and enforced daily temporal sequence.

**Provenance:** The approved specification is the "Upgraded Plan: Source-Frozen Qualification and Five-Market-Day Paper Trial" review (2026-08-22). This plan decomposes its Task 4.5 into six independently verifiable source increments. It supersedes the conflicting post-qualification `research shadow-streak-report` step recorded in `.superpowers/sdd/2026-08-17-clean-day-qualification-and-five-day-paper-trial/task-4-report.md`.

**Baseline:** Canonical `master` clean at `1743545`; this isolated worktree branch `codex/source-frozen-shadow-protocol-20260822`. Tasks 1–4 of the clean-day plan are complete. All ten automations are `PAUSED`; frozen live-control SHA-256 `a3fc5ddb2b300596833c48c1554fad088ecb43d46bd70aeeac074531d6e9fb07`; no qualification day or trial has run. The next calendar candidate remains Monday, 2026-08-24, but it must not be used until all six increments below are implemented, reviewed, merged to canonical, and freshly verified.

## Global boundaries (every increment)

- Source-only work in this isolated worktree; no canonical-master or other-worktree edits.
- All ten TradingAgents automations remain `PAUSED`; no automation update call.
- `TA_LIVE_SUBMIT=0` for every command; no live order/cancellation; no broker read or write from these tasks (tests use fakes only).
- No live-control refresh/rearm/unfreeze/rewrite; no promotion, risk, capital, account, credential, endpoint, schedule, or outbox change.
- No scheduled/unattended execution; no runtime qualification/trial activity.
- Every behavior change begins RED, then minimal GREEN, then focused + dependent verification.

## Verification vocabulary (per increment)

Focused: the increment's named test functions. Dependent: the owning modules' full test files plus listed neighbor suites. Static: `ruff check` on changed paths, `compileall -q` on changed packages, `git diff --check`. Program-wide final gate (Increment 6): full pytest, Ruff on `cli tradingagents scripts tests`, `compileall -q cli tradingagents scripts`, `git diff --check`, `zsh -n scripts/mac/ta_job.sh`, broker/network mutation inventory, independent specification review, independent authority/security review, zero unresolved P0/P1, fast-forward only from clean canonical `master`, then re-index the canonical graph.

---

## Increment 1: Correct terminal-report semantics and add read-only status

**Problem:** `build_shadow_streak_report()` (`tradingagents/evals/shadow_trial.py:2932`) admits a terminal `readiness_candidate`/`readiness_no_go` record; afterwards `_LedgerState` rejects any further start (`tradingagents/evals/shadow_trial.py:2530`, `2422`). The runbook calls it right after qualifier adjudication, terminally closing the program before Day 1.

**Files:**
- Modify: `tradingagents/evals/shadow_trial.py`
- Modify: `cli/main.py`
- Modify: `tests/test_shadow_trial.py`
- Modify: `tests/test_alpaca_cli.py` (runbook parser test)
- Modify: `.superpowers/sdd/2026-08-17-clean-day-qualification-and-five-day-paper-trial/task-4-report.md`

**Interfaces:**
- New `shadow_streak_status() -> dict`: pure replay via `_store_envelopes()` + `_LedgerState`; appends nothing. Returns always `{analysis_only: true, execution_authority: "none", can_submit_orders: false}` plus `phase`, `last_result` (object/run/market date/role/status/phase/reasons or null), `clean_trial_streak` (consecutive trailing clean trial-role days), `required_clean_trial_days: 5`, `pending_start` view or null, `predecessor_object_id` expected by the next start, `can_start_next_day`, and `ledger_head_object_id`.
- `build_shadow_streak_report(*, final_no_go: bool = False)`: refuses (ValueError) unless the ledger already ends in a candidate chain (clean `trial_complete`) or an existing report is being re-read, unless the caller explicitly passes `final_no_go=True` (deliberate program termination as NO-GO).
- CLI: new read-only `research shadow-streak-status [--json-output]`; `research shadow-streak-report` gains `--final-no-go/--no-final-no-go` and fails closed pre-terminal without it.
- Runbook: post-qualifier invocation replaced with `shadow-streak-status`; terminal `shadow-streak-report` documented exactly once, only after clean Day 5 adjudication + trial finalization.

**Tests (RED first):**
- `tests/test_shadow_trial.py::test_red_shadow_streak_status_is_read_only_non_authorizing_and_admissibility_true` — empty ledger, pending start, and post-qualification states report correct phase/streak/predecessor/admissibility; ledger byte state identical before/after status.
- `tests/test_shadow_trial.py::test_red_status_after_clean_qualification_does_not_block_trial_day_1` — status between qualifier and Day 1 leaves zero final-report objects and Day 1 start+adjudication succeed; streak counts trial days only.
- `tests/test_shadow_trial.py::test_red_terminal_report_refuses_before_clean_trial_complete_unless_final_no_go` — refusal after failed qualifier and mid-trial; `final_no_go=True` admits `readiness_no_go`; full clean chain still admits `readiness_candidate` without the flag.
- Update `test_red_production_cli_exposes_only_pinned_non_authorizing_inputs` signature assertion to require exactly one keyword-only parameter `final_no_go` defaulting False; add CLI-level status JSON assertions and pre-terminal CLI refusal.
- Update `tests/test_alpaca_cli.py::test_clean_day_runbook_exact_commands_exist_without_executing_runtime`: documented order becomes adjudicate < finalize < `shadow-streak-status` < exactly-one `shadow-streak-report`.

- [x] Write RED tests; record failures.
- [x] Implement `shadow_streak_status`, `final_no_go` gate, CLI command/flag, runbook revision.
- [x] Run focused shadow + runbook-parser tests, dependent `tests/test_alpaca_cli.py` shadow/sentinel cases, Ruff, compileall, `git diff --check`.
- [x] Commit Increment 1.

## Increment 2: Crash-safe day closure (abort + expire-pending)

**Problem:** A pending start blocks all future starts, but normal adjudication refuses after the Central date changes (`tradingagents/evals/shadow_trial.py:2731`). A crash can permanently strand the ledger.

**Files:**
- Modify: `tradingagents/evals/shadow_trial.py` (new semantic facades beside `create_shadow_day_start_manifest`)
- Modify: `cli/main.py`
- Test: `tests/test_shadow_trial.py` (new RED classes)

**Interfaces:**
- `abort_shadow_day(*, start_object_id: str, stopped_at_stage: str, notes...) -> EvidenceAdmission`: today's authenticated pending start only; admits terminal `failed`/`incomplete` result with `phase="repair_required"`, captured stop stage and existing-artifact inventory; never `clean`; no broker/schedule/control authority; reuses the anchored admission transaction.
- `expire_pending_shadow_day() -> EvidenceAdmission | None`: pending start whose Central market date is in the past converts to immutable `incomplete` with reason `pending_day_expired_without_adjudication`, phase `repair_required`; cannot manufacture evidence; idempotent-safe (returns None when nothing expired).
- CLI: `research shadow-day-abort --start-object-id ID --stopped-at-stage KEY ...` and `research shadow-day-expire-pending`.

**Tests:** interruption after every daily stage including immediately post-start, post-paper persistence, post-manifest, pre-adjudication; abort refuses wrong object id, non-today dates, existing successor; expire refuses same-date pending; both fail closed on anchor/journal damage; aborted/expired days reset streak and force repair→requalification transitions through `_start_spec`.

- [x] RED interruption-matrix and guard tests; record failures.
- [x] Implement both facades + CLI; prove every pending start has exactly one recoverable path to an immutable non-clean result.
- [x] Focused shadow tests + recovery suites + static checks; commit.

**Implementation record (2026-08-22):** Day payloads gained `closure_kind`, `stopped_at_stage`, and `notes` (exact-field schema). `SHADOW_DAY_STOP_STAGES` is `("day_start", *DAILY_CHAIN_STAGES, "manifest_written")`; `manifest_written` is abort-only and names the interruption window after the chain manifest is sealed but before its single adjudication. Abort forces reason `shadow_day_aborted_by_operator`; expiry forces `pending_day_expired_without_adjudication`; both derive `phase="repair_required"` through the unchanged `_day_phase`. Replay validation now admits a late day result only for `closure_kind="pending_expired"` recorded on a strictly later Central date. The three prior inline anchored-write blocks were extracted verbatim into the shared `_admit_shadow_envelope` transaction used by start, adjudicate, report, abort, and expire alike. CLI gained read-surface-only `research shadow-day-abort` (start id, stop stage, required notes, optional present-artifact paths) and argument-free `research shadow-day-expire-pending` (`expired: false` JSON when nothing qualifies). No runtime identity capture was added in this increment.

**Review fix round (P2, 2026-08-22):** removed an unreachable duplicated return at the end of `build_shadow_streak_report`; made the idle expiry probe observationally pure by replaying through `_store_envelopes_readonly()` before any admission, so a fresh or non-expired ledger creates no trusted-head lock and mutates no byte (RED: fresh-empty-ledger snapshot test failed against `a6054c8` because `.manual-shadow-trusted-head.lock` was created); added the abort-only `manifest_written` stop stage with interruption-matrix coverage via parametrization over `SHADOW_DAY_STOP_STAGES` (15 cases).

## Increment 3: Bind runtime identity

**Problem:** Start payloads pin control/schedule/calendar but not Git commit, dependency locks, interpreter, package inventory, model routes, or contract hashes; Days could silently drift across source versions.

**Files:**
- Create: `tradingagents/evals/runtime_identity.py`
- Modify: `tradingagents/evals/shadow_trial.py` (`_START_FIELDS`/`_DAY_FIELDS` gain `runtime_identity`)
- Modify: `cli/main.py`
- Tests: `tests/test_runtime_identity.py` (new), `tests/test_shadow_trial.py`

**Interfaces:**
- `capture_runtime_identity(...) -> dict`: canonical repo path; exact commit; `tracked_tree_clean=true`; SHA-256 of `pyproject.toml`, `uv.lock`, `requirements.txt`, `requirements-crawler.txt`; Python executable path/version; normalized installed-package inventory SHA-256; schedule/role-contract hashes; nonsecret overnight model/provider route names; packet-schema versions.
- Start and day payloads embed identical `runtime_identity`; cross-day mismatch is a hard failure (`runtime_identity_mismatch`); dirty tree or unparseable capture refuses start admission.

**Tests:** dirty tree; changed commit; changed lockfile/package inventory/model route/schedule TOML/live-control bytes; cross-day mismatch — each fails closed and counts as non-clean; documentation-only change also resets strict identity.

- [x] RED identity-capture and mismatch tests; implement capture + binding; verify; commit.

**Implementation record (2026-08-22):** Runtime identity is the canonical flat `runtime_identity/v1` payload; the obsolete nested `runtime_identity_v1` shape is rejected by strict validation. `validate_runtime_identity` enforces the exact field set with strict types, verifies every component digest (required-source map, contract/control hashes, automation-TOML map, provider-route/schema-version maps, overnight route, package inventory) plus the whole-payload `identity_sha256`, and structurally rejects empty required-source/automation-TOML/schema-version mappings even when all digests are freshly recomputed (`provider_routes` may remain empty because the production seam supplies no provider-route map). Git capture scrubs inherited `GIT_*` routing environment, pins `GIT_CONFIG_NOSYSTEM`, `GIT_TERMINAL_PROMPT`, and `GIT_OPTIONAL_LOCKS`, disables optional locks via `core.optionalLocks=false`, bounds each subprocess at 30 seconds, translates Git/path/read failures into `RuntimeIdentityError`, and pins `--untracked-files=all` on the command line so hostile user-global config such as `status.showUntrackedFiles=no` cannot hide dirt from the clean-worktree truth. External automation TOMLs are hashed under an explicit optional `automation_root` containment boundary requiring regular non-symlink files inside it; repository files, contracts, and live control stay constrained to `repo_root`. The no-argument private production seam `_runtime_identity_capture` binds exactly the task worktree with its four required source files (`pyproject.toml`, `uv.lock`, `requirements.txt`, `requirements-crawler.txt`), the canonical schedule/role contracts plus frozen live control, exactly ten canonical automation TOMLs, the current Python interpreter plus a deterministic normalized installed name==version inventory, the configured allowlisted provider/quick-model/deep-model names resolved with existing `TRADINGAGENTS_OVERNIGHT_*` environment/default precedence (backend URLs, CLI-only argument layers, health-probed auto-Ollama model overrides, and disabled-graph fallbacks are deliberately excluded as unobservable execution inputs), and the pinned `START_SCHEMA`/`DAY_SCHEMA`/`REPORT_SCHEMA` constants. Start captures and strictly validates identity before any ledger surface is initialized and admits nothing when the runtime is dirty, unavailable, malformed, internally inconsistent, or does not bind the captured control/schedule source digests; normal adjudication stores one fresh strict identity in the day payload and fails the result with phase `repair_required` and the single hard reason `runtime_identity_mismatch` on drift versus the admitted start, the immediate predecessor-day identity, or this day's bound control/schedule sources; replay compares only stored start/day/predecessor identities and never recaptures a changed worktree; abort and expiry copy the admitted start identity verbatim without calling the seam, expiry records exactly `["pending_day_expired_without_adjudication"]`, and closure reasons otherwise remain unchanged except for stored-predecessor drift. P2 review fixes: hostile global Git status configuration pinned (RED-to-GREEN via isolated fake HOME/gitconfig) and the overnight route locked to explicitly bounded configured-route semantics with focused precedence coverage. Verification under `TA_LIVE_SUBMIT=0`: 54 runtime-identity tests, 7 focused `-k runtime_identity` shadow cases, 95 full shadow-trial tests, 1 focused clean-day runbook CLI test, Ruff on all four changed source/test paths, compileall, and `git diff --check` all green. Final independent specification plus quality/security review: no P0/P1/P2 findings; P3 items are deferred advisory only. `cli/main.py` was intentionally left unchanged in this increment. Commit subject: `feat(evals): bind runtime identity to shadow days`.

## Increment 4: Market-day leases replace calendar-day leases

**Problem:** `paper_tournament.py:942` computes `ends_at = now + timedelta(days=duration_days)`; a midweek start expires before five market days elapse; holidays worsen it.

**Files:**
- Modify: `tradingagents/brokers/paper_tournament.py`
- Modify: `cli/main.py`
- Test: `tests/test_paper_tournament.py`

**Interfaces:**
- `authorized_market_day_limit` derived lease: `authorized_market_dates` computed at initialization from authenticated exchange calendar entries with real open/close/early-close times; `ends_at` = close of fifth authorized market date bounded by a conservative wall-clock ceiling; early closes come from calendar data, never fixed inference; admission uses the admitted dates list rather than elapsed wall clock.

**Tests:** Monday–Friday starts, weekend crossing, holiday crossing, early close, same-day duplicate, sixth market day, date outside admitted calendar, changed/unavailable calendar evidence — each fails closed.

- [x] RED lease-calendar tests written and recorded failing (25/25 new cases failed pre-implementation).
- [x] GREEN implemented: admitted-dates derivation, market-day admission, ceiling-bound `ends_at`, strict live-evidence cross-check, evidence-hash binding.
- [x] Focused + dependent paper/supervisor suites green; Ruff/compileall clean; `git diff --check` clean.
- [x] Commit Increment 4 — authorized following completed acceptance: independent verification PASS, specification re-review PASS, and quality/security review with no P0/P1 findings (legacy-ledger submission-ineligibility P2 documented and resolved in the implementation record); the scoped Increment 4 commit is cleared for execution on this worktree.

**Implementation record (2026-08-22):** Canonical market-day lease decision — `initialize_tournament(..., market_calendar=...)` admits exactly `max_submission_market_days` authenticated exchange sessions as `authorized_market_dates` (strict `{date, open, close}`, exchange-time session bounds converted to UTC instants), selected ascending from the init-date Central window returned over horizon `authenticated_market_date_window(now, duration_days, market_day_limit)` spanning `max(duration_days − 1, 2 × limit)` days so any configured capacity is admittable from plausible calendars while `duration_days` never gates admission. `ends_at = min(final admitted close UTC, started_at + lease_window_days)` with `lease_window_days = duration_days`; `authorized_market_dates`, `authorized_market_day_limit`, and `lease_window_days` are all bound into `_submission_lease_evidence`. `_validate_submission_lease` fails closed on missing/malformed/duplicated/out-of-limit admitted lists; rejects any Central date outside the admitted list (weekend, holiday, sixth market day, pre-window) before any broker call; requires the live `list_calendar(start=end=today)` response to contain a normalized matching admitted entry — every observation must be well-formed and uniquely dated, and the matched entry must equal the stored admitted evidence verbatim; malformed or duplicate observations and missing/changed/unavailable evidence each fail closed; enforces the regular-session window from the admitted entry's real open/close (early closes honored from data, never fixed inference). The fixed 8:30–15:00 CT window, weekday check, and elapsed-wall-clock expiry were removed — expiry is purely market-day based, with the conservative wall-clock ceiling enforced as `ends_at`. CLI `alpaca paper-tournament init` fetches the wide authenticated range and passes it through. Accepted quality-review P2 rationale: ledgers lacking `authorized_market_dates`/`lease_window_days` (pre-Increment-4 roots) are permanently submission-ineligible by design — such a root is normally rejected first because its legacy lease-evidence digest no longer matches, and any re-signed or otherwise malformed attempt then fails closed on its missing admitted market-day calendar evidence — and such roots must be replaced by a newly initialized trial root via `alpaca paper-tournament init`; no migration path is provided. Focused verification: `pytest tests/test_paper_tournament.py -q` → 87 passed (TA_LIVE_SUBMIT=0, fakes only, zero network). Dependent suites: `test_alpaca_cli.py` + `test_alpaca_supervisor.py` → 211 passed; Increments 1–3 integrity (`test_shadow_trial.py` + `test_runtime_identity.py`) → 149 passed; combined five-suite gate → 447 passed. Static: Ruff clean on the three changed files; `compileall -q tradingagents/brokers cli` clean; `git diff --check` clean. Final acceptance: independent verifier PASS; specification re-review PASS; quality/security review found no P0/P1 findings, and its legacy-ledger P2 is documented/resolved above. The worktree remains intentionally uncommitted until the Increment 4 commit is executed.

## Increment 5: Cross-bind paper and reconciliation evidence; retire abandoned ledgers

**Problem:** Manifest records paper IDs and embeds reconciliation separately (`shadow_trial.py:2070` area); nothing proves one-to-one order identity, post-paper ordering, or terminality; an abandoned paper root has no fail-closed retirement.

**Files:**
- Modify: `tradingagents/evals/shadow_trial.py` (`_manifest_reasons`/paper-stage reasons)
- Modify: `tradingagents/brokers/paper_tournament.py` (finalize/recovery-required retirement)
- Modify: `cli/main.py` (`alpaca paper-tournament abort-submission-lease` fail-closed op)
- Tests: `tests/test_shadow_trial.py`, `tests/test_paper_tournament.py`

**Interfaces:**
- Strict comparison inside manifest validation: every `ta-paperbot-*` paper-packet order appears exactly once in the post-paper snapshot with matching broker id, client order id, symbol, side, type, timestamp; duplicates/missing/extra/reverse timestamps/nonterminal/ambiguous ⇒ `incomplete`; reconciliation `captured_at` must follow the last paper submission record; naturally empty signal set stays clean.
- Recovery-required roots permanently reject further submissions; replacement trials get fresh roots/tournament IDs.

- [x] RED cross-binding + retirement tests written and recorded failing (13/13 binding-corruption cases returned clean pre-GREEN; stray-order case returned clean; abort CLI case failed with "No such command 'abort-submission-lease'"; unit-guard case raised `AttributeError: abort_submission_lease`). The matching-evidence positive fixture and the pre-GREEN refusal CLI case passed vacuously before implementation and are retained as regression guards.
- [x] GREEN implemented; verification (`TA_LIVE_SUBMIT=0`, worktree `PYTHONPATH`, canonical venv): `tests/test_paper_tournament.py` 90 passed; `tests/test_shadow_trial.py` 110 passed; dependent `tests/test_alpaca_cli.py` 125 passed and `tests/test_alpaca_supervisor.py` + `tests/test_strategy_shadow_attestation.py` 320 passed; Ruff clean on all five changed paths; `compileall -q` clean on `tradingagents/evals tradingagents/brokers cli tests`; `git diff --check` clean. Commit deliberately deferred until after the independent no-edit verifier plus specification and quality/security reviews.

**Implementation record (2026-08-22):** Manifest validation gained `_manifest_reconciliation_binding_reasons`, wired into the `_manifest_reasons` **incomplete** bucket so every defect yields a non-clean day without failing the ledger. For each `ta-paperbot-*` order in the bound paper packet's `submitted` list it requires exactly one match in the reconciliation's paper snapshot `all_orders` with equal broker `id`, client order id, symbol (case-insensitive), side/type (normalized), and exact `created_at` instant; matched orders must carry a terminal status (`filled|canceled|expired|rejected`, mirroring finalization); any `ta-paperbot-*` entry still in `open_orders` is flagged; the reconciliation `captured_at` must be at or after the latest packet submission timestamp; non-list/malformed `all_orders`, duplicate observed identities, duplicate packet identities, missing, extra, mismatched, reverse-timestamped, nonterminal, or invalid-timestamp evidence each produce a dedicated `broker_reconciliation_*` reason (`unbound_tournament_order_present`, `missing_tournament_order`, `extra_tournament_order`, `duplicate_tournament_order`, `duplicate_paper_packet_order`, `mismatched_tournament_order`, `nonterminal_tournament_order`, `open_tournament_order_present`, `captured_before_last_paper_submission`, `paper_submission_timestamp_invalid`, `tournament_evidence_invalid`). A naturally empty tournament signal set with no stray observed orders remains clean. Producer support: `alpaca reconcile-observer --shadow-start-object-id` now additionally performs one read-only `list_orders(status="all", after=<market-date midnight America/Chicago>)` against the exact paper client and embeds the result as `paper.all_orders`; any transport failure lands in `paper.errors["all_orders"]`, forcing packet status `HOLD` and therefore stage failure downstream (fail closed). Unbound observer runs are unchanged. Retirement: `SUBMISSION_WINDOW_ABORTED = "aborted"` and `abort_submission_lease(ledger, *, reason, now)` in `paper_tournament.py` accept only a `recovery_required` qualification window whose transaction is also `recovery_required`, require a non-blank operator reason and a monotonic aware timestamp not earlier than start, the failed-at stamp, or any recorded submission, then permanently stamp both `submission_window_status` and the transaction status `aborted` plus an immutable `submission_abortion` record (aborted_at, trimmed reason, failed market date). Permanence without altering the lease-evidence hash inputs comes from the transaction stamp: every admission path revalidates the transaction, so flipping the window status back to `open` still raises in submit and finalize; re-abort refuses (status no longer `recovery_required`). New CLI op `alpaca paper-tournament abort-submission-lease --reason TEXT [--log-dir] [--json-output]` never constructs a broker client and makes zero broker calls; it writes a `paper_tournament_abort` packet under the same per-root file/thread lock. Replacement trials must initialize fresh roots and thereby fresh tournament ids; retired roots are permanently submission-ineligible.

**Verifier fix rounds (P1/P2 + hardening, 2026-08-22):** The independent verifier's P1 was real: `AlpacaRestClient.list_orders(self, status="open")` accepted no `after` keyword, so the bound observer's day-scoped read raised `TypeError` in production and was captured as `HOLD` evidence — cross-binding could never be produced against the real wrapper (tests only passed because fakes tolerated the kwarg). A first fix followed an undocumented `page-token` cursor protocol; a second verifier documentation check invalidated that design (the official Get All Orders contract documents `status`, `after`, `until`, `direction`, `nested`, `symbols`, `side`, `asset_class`, `before_order_id`, `after_order_id`, and `limit` (default 50, maximum 500) with no cursor parameter), so the cursor transport and all pagination claims were removed. Final GREEN retrieval design: `_AlpacaRestClient.list_orders(status="open", *, after=None, limit=None)` issues one documented-parameter read (`status`, optional `after`, optional `limit`) and keeps the historical single-read behavior for existing status-only callers; with an explicit `limit` (validated as an exact int 1–500 before any transport) the read becomes a completeness proof — a collection strictly below the limit is provably whole because the broker fills up to the limit when more orders exist, while a collection that reaches the limit is ambiguous and raises `AlpacaExecutionError` ("order completeness cannot be proven"). The bound observer now requests exactly `status="all"`, `after=<market-date midnight America/Chicago>`, `limit=500` (`cli.main._BOUND_RECONCILIATION_ORDER_LIMIT`); ceiling or transport failures land in `paper.errors["all_orders"]`, forcing packet status `HOLD` and stage failure downstream (fail closed); unbound observer runs are unchanged. RED-first wrapper tests live in `tests/test_alpaca_execution.py`: `test_red_list_orders_requests_documented_filters_and_limit_without_cursor` (asserts the exact captured params `{status, after, limit}` and that no unsupported cursor key is ever sent), `..._valid_collection_below_limit_is_returned_complete`, `..._fails_closed_when_collection_reaches_the_ceiling` (exactly-limit result refuses), `..._rejects_invalid_bounds_before_transport[0|-1|501|"500"|True|2.5]` (no request issued), plus retained guards `..._rejects_blank_after_filter_before_transport` and `..._status_only_callers_keep_single_read_compatibility`. The pre-fix cursor suite failed RED with `TypeError: unexpected keyword argument 'after'`; this round's bounded-retrieval cases failed RED with `TypeError: unexpected keyword argument 'limit'` before the final implementation. Hardening: `mark_submission_recovery_required` now refuses an aborted window/transaction or any ledger carrying `submission_abortion` ("an aborted paper submission lease is permanently retired"), closing the direct-call revival path where a second recovery seal would flip a retired root back to `recovery_required` and allow re-abort history rewriting; `abort_submission_lease` independently refuses any ledger already carrying `submission_abortion`. Focused test: `tests/test_paper_tournament.py::test_red_mark_recovery_required_cannot_revive_an_aborted_root` (RED: revival succeeded and mutated the retired root; GREEN: both revivals raise and the ledger stays structurally equal to a deep-copied snapshot). Strict empty-day binding: `_manifest_reconciliation_binding_reasons` now requires, on zero-submission days, a mapping paper snapshot with parseable `captured_at` and present, well-formed `all_orders` (list of mappings); missing/malformed/ambiguous evidence yields `broker_reconciliation_tournament_evidence_invalid` (incomplete), stray tournament orders yield `broker_reconciliation_unbound_tournament_order_present`, and a genuinely empty day is clean only when valid full `all_orders` explicitly proves no ta-paperbot orders exist. RED cases `test_red_empty_day_requires_valid_full_order_evidence_to_stay_clean[empty-day-missing-all-orders|empty-day-malformed-all-orders|empty-day-nonmapping-all-order-entry]` failed clean pre-fix; the shared `_complete_daily_chain` fixture now embeds `"all_orders": []` so default quiet days prove emptiness. An invalid `captured_at` on an empty day continues to fail at the stricter pre-existing stage-semantics layer (`failed`, not incomplete) and was therefore not duplicated as a new reason.

**Verifier fix round 3 (P2-1/P2-2, 2026-08-22):** P2-1 resolved: `_manifest_reasons` now fails closed with `broker_reconciliation_embedded_copy_mismatch` whenever the manifest's embedded `broker_reconciliation` copy differs from the `stages["broker_reconciliation"]` payload it must mirror — a forged embedded copy could previously launder broken stage evidence (RED: `tests/test_shadow_trial.py::test_red_forged_embedded_reconciliation_cannot_launder_broken_stage_evidence` adjudicated **clean** while the captured stage file held an emptied order book; GREEN: the day is non-clean with the explicit mismatch reason). The day-adjudication helper gained a `mutate_manifest` hook so tests can doctor exactly the published manifest. Wording corrected: only a response strictly below the explicit limit proves completeness; an at-limit response fails closed (the `_BOUND_RECONCILIATION_ORDER_LIMIT` comment previously misstated this). P2-2 accepted scope, deliberately not broadened: cross-binding intentionally applies only to `ta-paperbot-`-prefixed client order ids, per the Increment 5 requirement; malformed or foreign client ids in paper evidence are not given new binding reasons because adjacent checks already fail that evidence closed (`paper_tournament_stage_submissions_invalid`, `paper_order_manifest_mismatch`, stage schema/hash validation), and widening the prefix filter would change non-increment behavior. No code change was made for P2-2.

**Reviewer P2 resolution — retired-root overwrite guard (2026-08-22):** Spec review found `alpaca paper-tournament init` could overwrite an aborted ledger in the same log dir, reviving a retired root in place. Narrowest durable fix: every `write_tournament_ledger` write now consults the on-disk target first (`_refuse_replacing_retired_root`) — if the persisted ledger is retired (`submission_window_status == "aborted"` or carries a mapping `submission_abortion`), the incoming ledger is accepted only when it carries the structurally identical (JSON-equality) retirement record **and** keeps `submission_window_status == "aborted"`; anything else raises `"paper tournament root is permanently retired; replacement trials must initialize a fresh root"`. This still permits writing the actual abort (the record does not exist on disk until that write), later same-ledger records such as report/equity updates (record preserved verbatim), and all non-aborted flows (open/recovery_required/finalized roots behave exactly as before); an **absent** prior file initializes normally, while an existing but unparseable or non-mapping ledger file fails closed with `ValueError: existing paper tournament ledger is unreadable or invalid` before any bytes are written — a corrupted retired ledger can therefore never be overwritten (reviewer P2 correction round; RED tests `test_red_write_refuses_invalid_existing_ledger_before_any_overwrite` and `test_red_cli_init_cannot_replace_a_malformed_existing_ledger` failed pre-fix because the overwrite succeeded, and prove the corrupt file stays byte-identical plus fresh absent-root directories still initialize open without any retirement record). Every production writer loads the live ledger before writing except init, so only init's fresh-ledger path can hit the refusal. RED-to-GREEN coverage: `tests/test_paper_tournament.py::test_red_write_refuses_replacing_an_aborted_root_ledger` (abort-write allowed, same-ledger rewrite allowed, fresh replacement refused, keep-record-but-flip-window revival refused, disk stays retired) and `test_red_cli_init_cannot_replace_an_aborted_root` (CLI re-init over an aborted root exits nonzero with the retired ledger structurally identical afterward; a genuinely fresh directory still initializes open without any retirement record). Both failed pre-fix (overwrite succeeded). Scope note: at-or-after `captured_at` comparison semantics for reconciliation evidence are intentionally not decided here — they are handled by Increment 6's causal stage-ordering enforcement.

**Final verifier P2 documentation pass (2026-08-22):** Wording corrected everywhere in this increment's scope: the durable retirement-record comparison and the manifest embedded-copy comparison are **structural JSON equality**, not byte-for-byte comparison (`_refuse_replacing_retired_root` docstring, the `shadow_trial.py` embedded-copy comment, and this record). Two design points recorded and justified explicitly. (1) Empty-day `all_orders` provenance: the evidence is bounded to the hash-bound `broker_reconciliation` stage artifact of the authenticated daily-chain manifest; its sole supported producer is `alpaca reconcile-observer --shadow-start-object-id`, which reads with only documented query parameters (`status="all"`, day-start `after`, `limit=500`) and lands any ceiling/transport failure in `paper.errors["all_orders"]` → packet `HOLD` → stage failure downstream. No independently signed or separately read order-receipt expansion is introduced in this increment; strengthening provenance beyond the hash-bound stage + single producer would add new protocol surface outside Increment 5's scope. (2) `_refuse_replacing_retired_root` concurrency posture: the check is safe only inside the per-root `tournament_submission_lock`; direct callers of the module-level helper are responsible for their own lock ownership, and no new internal locking is added inside the helper to avoid changing the established atomic write boundary. **Corrected by the concurrency audit round below** — at the time this sentence was first written, init and alphainsider-watch did not yet hold the lock; they do now.

**Concurrency audit round (verifier P2, 2026-08-22):** A fresh verifier found the prior lock claim false and a real TOCTOU: `paper-tournament init` and `alphainsider-watch` called `write_tournament_ledger` outside `tournament_submission_lock`, so an abort could land between the guard's disk read and the atomic write. Full audit of every durable CLI ledger-write site: run (five writes), finalize, abort, and report were already lock-held for their whole ledger sections; **init** wrote unlocked after its broker/calendar reads; **alphainsider-watch** loaded the ledger early outside any lock and later wrote that stale snapshot unlocked. Fixes: init now acquires the per-root lock around only the final durable initialization write (broker/calendar reads stay outside, as the verifier accepted); alphainsider-watch keeps its early snapshot solely as planning input (existing behavior preserved) and performs its persisted update under the lock with a fresh reload of the on-disk ledger — an interleaved abort is therefore respected, never overwritten, and the retired-root guard always sees live state; if the file disappears before the locked reload, the update degrades to the established absent-ledger behavior (plan packet still written, no ledger write). With these paths locked, guard read and atomic write share the lock in all seven production sites, so no lock-compliant abort can interleave between them. Fake-only deterministic tests (the earlier threaded-scheduler interleaving test was removed as brittle per verifier guidance; serialization follows from the proved lock spans plus every producer sharing the same per-root lock): `tests/test_paper_tournament.py::test_red_init_and_watch_hold_the_per_root_lock_across_guard_and_ledger_write` instruments acquire/guard/atomic-write/release and asserts the exact order `acquire → guard → write(paper-tournament-ledger.json)` with release only afterward, for both init and alphainsider-watch (RED pre-fix: neither command acquired any lock); `..._update_respects_an_aborted_root_under_lock` runs alphainsider-watch against an already-aborted root and asserts exit 0 with the retirement record structurally unchanged, window still aborted, plan added on the reloaded ledger, under the same proved lock span. The guard docstring's lock-reachability claim was rewritten to match this audited reality.

**Verifier P2 coverage-gap resolution (2026-08-22):** Two fake-only direct CLI tests close the reported coverage gaps without any behavior change (both passed immediately as regression locks of existing fail-closed paths): `tests/test_shadow_trial.py::test_bound_reconciliation_records_hold_evidence_when_all_orders_read_fails` proves that a bound reconcile-observer whose paper `list_orders(status="all", ...)` raises records `paper.errors["all_orders"] = "list_orders(all) failed: …"`, emits packet status `HOLD`, leaves no `all_orders` evidence behind, and that the same payload is non-clean-compatible downstream (`broker_reconciliation_stage_semantics_invalid` from `_stage_semantic_reasons`); `..._bound_reconciliation_winter_market_date_uses_cst_utc_offset` proves the derived day-start bound follows the market date's UTC offset — for CST date 2027-01-04 it asserts exactly `(status="all", after="2027-01-04T06:00:00+00:00", limit=500)`, complementing the existing CDT case. The shared test helper gained an additive `dead_man_expires_at` parameter so winter-date fixtures can outlive the default control expiry.

**Quality/security reviewer P2 resolution — open_orders evidence strictness (2026-08-22):** `_reconciliation_tournament_entries` previously treated a missing named list and silently skipped non-Mapping list items as a proven empty set. Hardened contract: every named snapshot list must be present in the mapping, be a list/tuple, and contain only Mapping items; any violation returns `None`, which both binding paths turn into `broker_reconciliation_tournament_evidence_invalid`. Empty-submission days therefore require present-and-valid `open_orders` evidence to stay clean, and non-empty days fail closed when `open_orders` evidence is absent or malformed. Stage semantics and output signatures are unchanged. RED→GREEN coverage in `tests/test_shadow_trial.py`: strict-path corruption cases `test_red_broken_paper_to_reconciliation_binding_is_never_clean[absent-open-orders-evidence|malformed-open-orders-entry]` failed pre-fix (clean accepted) and now yield **incomplete** with the explicit binding-invalid reason; quiet-day defects are layered — `test_red_empty_day_open_orders_defects_fail_closed_in_both_layers[absent|nonmapping-entry]` proves status **failed** via the pre-existing stage-semantics layer (`broker_reconciliation_stage_semantics_invalid`) while the hardened helper adds its own invalid-evidence reason as defense in depth; and `test_red_reconciliation_tournament_entries_require_present_mapping_lists` pins the helper contract directly (present valid lists collect ta-paperbot entries; absent key, non-list value, non-mapping item, and non-mapping snapshot each return `None`).

**Final verification (2026-08-22, `TA_LIVE_SUBMIT=0`, worktree `PYTHONPATH`, canonical venv):** `pytest tests/test_paper_tournament.py tests/test_shadow_trial.py tests/test_alpaca_execution.py -q` → 329 passed (`tests/test_shadow_trial.py` 121, `tests/test_paper_tournament.py` 97, `tests/test_alpaca_execution.py` 111); dependent `tests/test_alpaca_cli.py` + `tests/test_alpaca_supervisor.py` + `tests/test_safety_sentinel.py -q` → 238 passed; Ruff clean on all seven changed Python paths (`tradingagents/brokers/alpaca.py tradingagents/evals/shadow_trial.py tradingagents/brokers/paper_tournament.py cli/main.py tests/test_shadow_trial.py tests/test_paper_tournament.py tests/test_alpaca_execution.py`); `compileall -q tradingagents/brokers tradingagents/evals cli tests` → clean; `git diff --check` → clean. Commit remains deliberately deferred for independent verification and reviews.

## Increment 6: Enforce the daily temporal sequence

**Problem:** Verifier proves containment within start→adjudication but not causal monotonic order across the sixteen-step day.

**Files:**
- Modify: `tradingagents/evals/shadow_trial.py` (stage-order validation over manifest stages)
- Test: `tests/test_shadow_trial.py`

**Interfaces:**
- Calendar-derived stage windows plus monotonic nondecreasing `generated_at` ordering over `DAILY_CHAIN_STAGES` in the approved sequence (sentinel bound, hourly dry-run, loss review, BOARD, handoff, plan, paper tick during regular session, reconciliation after paper tick, daily report after session, manifest, adjudication); backward/causal-reversed timestamps fail; equality allowed only where producer precision legitimately ties.

**Tests:** pairwise swaps, equal-timestamp acceptance, backward timestamps, paper-after-reconciliation reversal, out-of-session paper tick — each non-clean.

- [x] RED ordering tests written and recorded failing (30/34 new cases failed pre-GREEN because corrupted chains were still admitted **clean** — no temporal/causal validation existed; the 4 valid-chain positive fixtures passed vacuously and are retained as regression guards).
- [x] GREEN implemented as fail-closed manifest-validation-only change (no CLI, schema, or public-signature changes); all 34 new cases green, full `tests/test_shadow_trial.py` 155 passed.
- [x] Verified (`TA_LIVE_SUBMIT=0`, worktree `PYTHONPATH`, canonical venv): dependent `tests/test_alpaca_cli.py` 125 passed and `tests/test_paper_tournament.py` 97 passed; Ruff clean on both changed paths; `compileall -q tradingagents/evals tests` clean; `git diff --check` clean. Commit deliberately deferred for independent verification and reviews.

**Implementation record (2026-08-22):** Manifest validation gained `_stage_sequence_reasons`, wired into the `_manifest_reasons` **incomplete** bucket so every defect yields a non-clean day without failing the ledger, consistent with existing timestamp-window semantics. The approved causal order is the new module constant `DAILY_CHAIN_TEMPORAL_ORDER`: overnight research → premarket brief → preopen validation → safety sentinel → hourly supervisor → loss review → execution board → self-heal handoff → self-heal plan → paper tournament → broker reconciliation → daily report. It deliberately differs from the roster tuple order in two places that match the frozen automation schedule: the sentinel bind (:20) precedes the hourly dry-run (:35), and paper tick → reconciliation → daily report replaces the roster's daily-report/reconciliation/paper tail. Consecutive pairs must be strictly increasing in `generated_at`; equality is legitimate only for the documented same-process producer tie `("self_heal_handoff", "self_heal_plan")` (one self-healer automation run writes both packets back-to-back at second resolution) — any other consecutive tie is `daily_chain_stage_order_tie_illegitimate`; any backward consecutive pair is `daily_chain_stage_order_backward`. Because consecutive-pair monotonicity is enforced over the approved sequence, every pairwise/causal reversal (including the called-out paper-after-reconciliation swap and non-adjacent swaps) necessarily breaks at least one consecutive pair and is caught. The sealed manifest must not precede any bound stage stamp (`daily_chain_manifest_precedes_stage`); manifest-equal-to-last-stage ties remain legitimate because sealing reads already-written files within the same second, and adjudication-after-manifest was already enforced by the existing `daily_chain_manifest_timestamp_out_of_window` containment check (equality allowed). Session windows are derived exclusively from the authenticated start-day calendar evidence via `_admitted_regular_session_bounds` — exactly one admitted session entry for the market date carrying strict HH:MM exchange wall times (mirroring the Increment 4 paper-lease convention, America/New_York), so early closes come from data and DST is handled by zone conversion, never fixed inference; missing/malformed window evidence fails closed with `daily_chain_session_window_unavailable`. The paper tick must satisfy open ≤ generated_at < close (`paper_tournament_stage_outside_regular_session`) and the daily report must be at or after the session close (`daily_report_stage_before_session_close`; report-at-close-second equality allowed as a nondecreasing chain continuation). All rejection reasons land in the incomplete bucket: status becomes `incomplete` (phase `repair_required`), never clean. Test-side fixtures now mirror producer reality: `_calendar()` and `_CalendarFake` emit strict HH:MM session entries, `_stage_timeline()` stamps an ascending producer-real UTC chain whose minutes mirror the canonical Central offsets (valid across CDT and CST dates), and default adjudication happens after the daily-report stamp (`_DAY_CLOCK_HOUR`). New coverage (34 cases): valid ascending chain regression; adjacent-pair backward/swaps plus a first↔last far swap; dedicated paper-after-reconciliation reversal; equal-timestamp acceptance only for the self-heal tie while all ten other consecutive ties fail; paper ticks before open / after close / after an early close with an inside-early-close clean control; daily-report before close / before early close with an at-close clean control; CST winter date proving the window derives from admitted calendar data rather than a fixed CDT offset; and manifest-cannot-precede-stage backfilling. Known limitation recorded by design: stages with unreadable or unparseable bindings contribute no timestamp to the sequence scan and are already failed closed by their own `{stage}_stage_*` reasons.

**Final-gate stale-pin maintenance (2026-08-22):** The independent verifier confirmed all Increment 6 behavior but blocked the final gate on `tests/test_authority_role_alignment.py::test_production_raw_http_mutation_inventory_has_no_unclassified_transport`, whose exact positional inventory still expected `cli/main.py:5244`/`:5256` and `tradingagents/brokers/alpaca.py:70`/`:83` while the scanner reports the same four reviewed mutations at `cli/main.py:5245`/`:5257` and `tradingagents/brokers/alpaca.py:72`/`:85` after committed Increment 5 line shifts. Reproduced RED locally (1 failed), verified each new position in source (`queue.put(` result/error queue writes; `self.__session.request(` GET/POST transport calls) so the classifications are unchanged, then applied the narrowest test-maintenance GREEN: updated exactly those four `(path, line, scope, mutation)` keys, preserving the deliberately position-exact inventory strictness (no semantic-loosening redesign). Source files untouched. Verification (`TA_LIVE_SUBMIT=0`, worktree `PYTHONPATH`, canonical venv): targeted inventory test 1 passed; full `tests/test_authority_role_alignment.py` + `tests/test_safety_sentinel.py` 80 passed; Ruff clean on `tests/test_authority_role_alignment.py`; `compileall -q tradingagents/evals tests` clean; `git diff --check` clean. The shadow-trial suites were not re-run because this round touches only the alignment-test file and no Increment 6 source or fixture path; the complete diff remains uncommitted for independent review.

**Spec-review P2 fix round (2026-08-22):** P2-1 — `_parse_exchange_wall_time` used bare `isdigit()` before `int()`, and Unicode digit characters defeat that pair two ways: superscript digits such as `²²:³³` satisfy `isdigit()` but raise `ValueError` inside `int()` (crashing adjudication instead of failing closed), while Arabic-Indic digits such as `١٦:٠٠` satisfy both and silently convert to a valid-looking time. RED evidence recorded by direct probe (`helper('²²:³³')` raised `ValueError: invalid literal for int() with base 10: '²²'`; `helper('١٦:٠٠')` returned `16:00:00`) plus new failing tests. Narrowest GREEN: require `isascii()` alongside `isdigit()` for both fields so any non-ASCII digit returns None and the existing `daily_chain_session_window_unavailable` fail-closed path applies; no other validation surface changed. New fake-only coverage (9 cases): strict-ASCII acceptance matrix (`09:30`, `16:00`, `23:59`) with malformed-shape controls (`9:30`, `09:60`) plus superscript, Arabic-Indic, and mixed ASCII/Unicode rejections at the private helper; and an end-to-end chain adjudication proving a start admitted with non-ASCII session walls (the calendar binding validates only the admitted date) yields status **incomplete** with `daily_chain_session_window_unavailable` instead of raising. P2-2 — corrected the winter DST comment in `tests/test_shadow_trial.py`: EST conversion of 09:30–16:00 ET is 14:30–21:00 UTC (was misstated as 15:30–21:00); test assertions were already correct and unchanged. Verification (`TA_LIVE_SUBMIT=0`, worktree `PYTHONPATH`, canonical venv): focused temporal/Unicode set 43 passed; full `tests/test_shadow_trial.py` 164 passed; dependent `tests/test_authority_role_alignment.py` 53 passed; Ruff clean on all three changed Python paths; `compileall -q tradingagents/evals tests` clean; `git diff --check` clean. The four-file diff remains uncommitted.

**Quality/security review P2 — recorded decision, documentation-only resolution (2026-08-22):** The fresh quality/security review accepted Increment 6's validation behavior but flagged its forward compatibility surface: because `_manifest_reasons` re-evaluates stored stage stamps under the *current* causal rules during deterministic replay, any future tightening of `DAILY_CHAIN_TEMPORAL_ORDER`, tie exceptions, or session-window semantics would retroactively recapture/reclassify already-admitted start/day evidence — replaying an older clean ledger could newly fail or change reasons without any new evidence, which would silently alter qualification streaks and phase transitions. Decision recorded here as a durable guardrail: later causal-rule tightening must NOT recapture or reclassify stored admitted start/day evidence during deterministic replay; stored evidence keeps the semantics it was admitted under, and any future tightening must ship as a separately versioned migration/backward-compatibility decision (explicit rule-version binding in payloads plus an explicit migration record), never as silent reinterpretation at read time. This is presently a forward guardrail only: no persisted manual-shadow envelope exists today (no qualification day or trial has run), so there is no stored ledger that future rules could reclassify, and Increment 6's fail-closed admission behavior for *new* days is unaffected. Related convention note: deriving each stage's recorded time from the binding record's own `generated_at` field was pre-existing, reviewed behavior from earlier increments (the manifest binds file identity plus payload, and the timestamp convention predates Increment 6), and remains accepted unchanged by this review. No source or test changes were made in that round.

**Final quality/security review P2 — cosmetic test-ID correction (2026-08-22):** The review found two parametrized case IDs in `tests/test_shadow_trial.py::test_red_paper_tick_outside_admitted_regular_session_is_never_clean` mislabeled `before-open-cdt` and `after-close-cdt` although their exchange-window semantics are America/New_York. Label-only fix: renamed to `before-open-et` and `after-close-et`; no fixture, assertion, stamp, or behavior change of any kind (the session window was already derived from admitted calendar data via `_EXCHANGE_TZ`, never a fixed Central assumption). Focused verification (`TA_LIVE_SUBMIT=0`, worktree `PYTHONPATH`, canonical venv): the renamed cases pass under their new IDs; `git diff --check` clean. Diff remains uncommitted.

---

## Final Task 4.5 acceptance (before any Task 5 runtime)

- [ ] Increments 1–6 committed; full pytest green; Ruff/compileall/diff/wrapper checks green; mutation inventory reviewed.
- [ ] Independent specification review and independent authority/security review with zero unresolved P0/P1 findings.
- [ ] Fast-forwarded to clean canonical `master`; canonical graph re-indexed with coverage for every changed path.
- [ ] Only then select the first real market day for Task 5A campaign admission.
