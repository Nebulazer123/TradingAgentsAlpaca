"""Supervisor concern modules.

This package holds pure supervisor responsibilities that used to live inside
``tradingagents.brokers.alpaca_supervisor``. The legacy module still re-exports
the public helpers so existing CLI/tests/imports keep working.
"""

