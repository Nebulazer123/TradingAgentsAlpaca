# TauricResearch v0.3.1 Selective Port Manifest

Pinned source:

- Checkout: `/tmp/tradingagents-upstream.VojTjl`
- Commit: `01477f9afb7a47b849ed4c9259d3a9a4738d9fda`
- Tag: `v0.3.1`

The source is a reference, not a replacement tree. `implementation_state`
distinguishes completed adaptations from work that remains pending or deferred.

| commit | upstream_subject | priority | local_status | decision | implementation_state | local_files | upstream_tests | focused_proof | reason |
|---|---|---:|---|---|---|---|---|---|---|
| `b47a828` | fix(graph): give the shared debate/risk routers a complete path_map | P0 | equivalent | adapt | adapted | `tradingagents/graph/setup.py`; `tests/test_risk_router_path_map.py` | `tests/test_risk_router_path_map.py` | Router/manifest/concurrency/tool routing: 17 passed. Full suite: 1,490 passed, 1 skipped, 75 subtests passed. | Uses complete shared identity maps while preserving local `Send` fan-out, concurrency limits, tool-free/prefetched factories, branch-local message IDs, and cleanup. |
| `daf1da9` | fix(graph): key checkpoints on graph shape and expose the LLM retry budget | P0 | superseded | adapt | adapted | `tradingagents/graph/checkpointer.py`; `tradingagents/graph/trading_graph.py`; `tests/test_checkpoint_resume.py` | `tests/test_checkpoint_resume.py`; `tests/test_llm_max_retries.py` | Checkpoint/provider/original workflow: 52 passed. Full suite: 1,526 passed, 1 skipped, 75 subtests passed. | Uses a frozen, versioned, canonical graph-shape signature with explicit-only legacy access and pre-side-effect validation. Strict programmatic retry validation preserves local default `2`, explicit `0`, and provider-neutral forwarding without accepting upstream numeric-string or float coercion. |
| `7df18fc` | refactor(data): unify vendor errors under a VendorError hierarchy | P0 | superseded | adapt | adapted | `tradingagents/dataflows/_official_common.py`; `tradingagents/dataflows/alpha_vantage_common.py`; `tradingagents/dataflows/alpha_vantage_fundamentals.py`; `tradingagents/dataflows/decision_vendor_adapters.py`; `tradingagents/dataflows/interface.py`; `tradingagents/dataflows/stockstats_utils.py`; `tests/test_dataflow_error_taxonomy.py`; `tests/test_alpha_vantage_hardening.py`; `tests/test_dataflows_interface.py`; `tests/test_yfinance_stale_ohlcv_guard.py` | `tests/test_vendor_errors.py` | Typed-taxonomy/router/official-data suite: 129 passed. Full suite: 1,701 passed, 1 skipped, 75 subtests passed. | Adapts the upstream principle to the local official-data boundary: only `RecoverableDataflowError` subclasses may fall through to another vendor. Missing optional configuration, exhausted transport, rate limits, open circuits, unusable upstream JSON, and specifically audited no-data cases are typed; raw `OfficialDataError`, raw transport exceptions, invalid inputs, malformed evidence, assertions, and programming failures remain terminal. Provider ordering, merge limits, health accounting, retries, redaction, and execution authority are unchanged. |
| `ee1ece3` | fix(dataflows): degrade gracefully when an optional vendor fails | P0 | superseded | adapt | adapted | `tradingagents/dataflows/_official_common.py`; `tradingagents/dataflows/fmp.py`; `tradingagents/dataflows/google_news.py`; `tradingagents/dataflows/reddit.py`; `tradingagents/dataflows/yfinance_earnings_calendar.py`; `tradingagents/dataflows/yfinance_options.py`; `tradingagents/dataflows/yfinance_short_interest.py`; `tradingagents/research/provider_orchestrator.py`; `tradingagents/research/twitter_mcp.py`; `tradingagents/research/youtube_transcript.py`; provider-containment tests | `tests/test_fred.py`; `tests/test_vendor_routing.py` | Strict containment/dataflow/research suite: 221 passed. Full suite: 1,755 passed, 1 skipped, 75 subtests passed. | Adapts graceful optional-source recovery to the local packet bundle without upstream's stale optional-category sentinel: only typed unavailable, transport, or missing-configuration outcomes may produce blocked evidence, stale fallback where explicitly allowed, a cache miss, or a later-provider attempt. Raw official-data, runtime, type, assertion, Pydantic, caller-validation, and cache-corruption failures abort unchanged. Corrupt cache cannot trigger a refresh, invalid dates fail before I/O, configured earnings-calendar routing is active without reordering, and all evidence remains analysis-only with no execution authority. |
| `9fd54f8` | fix(data): reject stale yfinance OHLCV instead of reporting wrong prices | P0 | superseded | adapt | adapted | `tradingagents/dataflows/stockstats_utils.py`; `tradingagents/dataflows/y_finance.py`; `tradingagents/dataflows/_official_common.py`; `tradingagents/research/provider_orchestrator.py`; `tests/test_yfinance_stale_ohlcv_guard.py`; `tests/test_stockstats_utils.py`; `tests/test_research_provider_orchestrator.py` | `tests/test_yfinance_stale_ohlcv_guard.py`; inclusive-end regression from `dab0768` | Stale-OHLCV/dataflow/provider/cache/manifest suite: 118 passed. Full suite: 1,676 passed, 1 skipped, 75 subtests passed. | Uses one pure ten-calendar-day validator across direct history, the 15-year indicator cache, direct research packets, official-cache admission, and provider-specific fresh-cache hits. Empty, date-less, future, and stale frames fail with `OfficialDataError`; historical checks use their supplied as-of date; downloaded stale frames are rejected before persistence; yfinance's exclusive end is made inclusive without rewriting symbols; actual final-bar time drives packet freshness; invalid yfinance cache cannot hit or stale-fallback and the unchanged provider chain continues. |
| `3570f2e` | fix(dataflows): apply the Alpha Vantage fundamentals look-ahead filter | P0 | superseded | adapt | adapted | `tradingagents/dataflows/alpha_vantage_fundamentals.py`; `tests/test_alpha_vantage_hardening.py`; `tests/test_dataflows_interface.py` | `tests/test_alpha_vantage_hardening.py` | Alpha Vantage/dataflow/official-adapter/manifest suite: 94 passed. Full suite: 1,638 passed, 1 skipped, 75 subtests passed. | Parses the vendor's JSON-text statement payload and retains a row only when its exact fiscal date and every supplied recognized availability date are on or before the cutoff. Historical rows require availability evidence, while a current/future fetch may prove missing availability. Invalid cutoffs fail before I/O; unusable historical statements and current-only historical OVERVIEW snapshots raise `OfficialDataError` for the unchanged router to fail over. No reporting lag is guessed, and live string passthrough remains compatible. |
| `9ad98c5` | fix(data): normalize ticker on the news path | P0 | missing | port | pending | None in this slice. | `tests/test_symbol_normalization_paths.py` | Pending. | Selected news normalization has not been ported. |
| `517eeaf` | fix(structured): harden structured output for local servers and thinking models | P0 | partial | adapt | adapted | `tradingagents/agents/utils/structured.py`; `tests/test_structured_agents.py` | `tests/test_openai_compatible_provider.py`; `tests/test_structured_agents.py` | Structured/capability/provider suite: 116 passed. Full suite: 1,603 passed, 1 skipped, 75 subtests passed. | Explicit parsed `None` now bypasses the renderer and deliberately retries the same prompt through the plain LLM while valid parsed objects retain the markdown renderer path. The upstream generic `openai_compatible` registry was rejected in favor of the local capability-aware provider layer. The fallback result is plain prose, but the existing prose-to-`SignalProcessor.parse_rating` path remains; executable-authority isolation belongs to the later typed decision compiler, not this parsing slice. |
| `0405168` | fix(schema): coerce null-ish strings in optional float fields | P0 | superseded | adapt | adapted | `tradingagents/agents/schemas.py`; `tests/test_structured_agents.py` | `tests/test_structured_agents.py` | Structured/capability/provider suite: 116 passed. Full suite: 1,603 passed, 1 skipped, 75 subtests passed. | Applies coercion only to `TraderProposal.entry_price`, `TraderProposal.stop_loss`, and `PortfolioDecision.price_target`: `None`, empty/whitespace, `null`, and `None` markers mean absence after case-folding and stripping; finite numeric strings parse normally. Booleans, NaN, infinities/overflow, objects, arrays, arbitrary text, and broad placeholders such as `N/A`, `-`, and `TBD` fail validation instead of becoming data. |
| `622f99d` | fix(analysts): align the news prompt with the get_news tool signature | P0 | equivalent | port | ported | `tradingagents/agents/analysts/news_analyst.py`; `tests/test_news_analyst_prompt.py` | `tests/test_news_analyst_prompt.py` | News prompt plus asset-context, graph routing/concurrency, capability, LLM-client, dataflow, and manifest preservation: 50 passed. Full suite: 1,605 passed, 1 skipped, 75 subtests passed. | The prompt now advertises the real ticker-based `get_news` contract and news by ticker symbol while preserving local macro, insider, instrument-context, prefetched/provider, concurrency, and tool-routing behavior. |
| `a0120e1` | feat(reporting): share the report-tree writer between the CLI and the API | P1 | partial | adapt | pending | None in this slice. | `tests/test_reporting.py` | Pending. | Planned reporting adaptation has not been claimed complete. |
| `eeb84aa` | fix(reddit): go RSS-first with 429 backoff and robust transport errors | P1 | partial | adapt | pending | None in this slice. | `tests/test_reddit_fallback.py` | Pending. | Planned Reddit adaptation has not been claimed complete. |
| `308757c` | fix(data): catch http.client transport errors in StockTwits | P1 | missing | port | pending | None in this slice. | `tests/test_stocktwits_resilience.py` | Pending. | Selected StockTwits resilience change has not been ported. |
| `a102afa` | fix(dataflows): map crypto to StockTwits/Reddit sentiment symbols | P1 | partial | adapt | pending | None in this slice. | `tests/test_reddit_fallback.py`; `tests/test_stocktwits_resilience.py`; `tests/test_symbol_utils.py` | Pending. | Planned crypto-symbol adaptation has not been claimed complete. |
| `ddfb840` | feat(data): add FRED macro indicators as an optional vendor | P1 | partial | adapt | pending comparison | None in this slice. | `tests/test_fred.py` | Pending comparison. | Local FRED already exists and must be compared before accepting upstream changes. |
| `db05903` | feat(data): add Polymarket prediction markets as a keyless vendor | P2 | missing | port | deferred | None. | `tests/test_polymarket.py` | Deferred. | Add only after P0 proof and only as non-executable advisory evidence. |
| `43bd32b` | feat(llm): support Bedrock API-key auth via AWS_BEARER_TOKEN_BEDROCK | P2 | missing | port | deferred | None. | `tests/test_bedrock_provider.py` | Deferred. | Add only when a real approved credential route is configured. |
| `a420ad0` | fix(cli): honor env precedence for LLM and run config | P1 | partial | adapt | pending | None in this slice. | `tests/test_anthropic_effort.py`; `tests/test_cli_config_precedence.py`; `tests/test_cli_env_skip.py`; `tests/test_env_overrides.py`; `tests/test_openai_reasoning_effort.py` | Pending. | Planned adaptation around the local CLI has not been claimed complete. |
