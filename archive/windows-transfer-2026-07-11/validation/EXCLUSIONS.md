# Transfer exclusions

Excluded from both source copies: real `.env` files and credential-bearing backups; `.venv` and `backend/.venv`; `node_modules`; `.git` metadata; Python and tool caches; `__pycache__`; `.pyc`; and other machine-specific dependency state. Example environment templates, lockfiles, manifests, source assets, generated project results, reports, deliverables, and documentation remain included.

TradingAgents Git provenance is preserved in `04_SETUP/SOURCE_STATE.json`, `SOURCE_STATE.md`, the uncommitted diff, diff statistics, and deleted-file list. MiroFish is documented as a non-Git source tree.
