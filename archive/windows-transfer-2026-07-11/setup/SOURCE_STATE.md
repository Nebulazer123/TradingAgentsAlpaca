# Source state receipt

The authoritative machine-readable source snapshot is `SOURCE_STATE.json`. TradingAgents is a dirty Git checkout on branch `wip/submit-path-hardening-2026-06-05`; its remote, HEAD, status, diff statistics, and deleted-file list are recorded there and in the neighboring diff artifacts. MiroFish has no root `.git`, so its provenance is represented by its original path, inventory, and captured source tree rather than Git metadata.

The source repositories were not reset, cleaned, committed, or overwritten during packaging.
